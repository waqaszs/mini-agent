# Anthropic API key — used to call Claude's Sonnet model.
# Get your own at https://console.anthropic.com/ , OR use the budget-capped key
# provided with this submission.
#
# Copy this file to `.env` and paste the key. `.env` is gitignored and never committed.
ANTHROPIC_API_KEY=
